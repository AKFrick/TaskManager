<Window x:Class="TaskManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManager"
        xmlns:views="clr-namespace:TaskManager.View"
        xmlns:modelViews="clr-namespace:TaskManager.ModelView"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <modelViews:MainVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!--Top panel-->
        <views:TopPanel Grid.Row="0"/>
        <Button Command="{Binding AddTask}" Content="AddTask" HorizontalAlignment="Left" Margin="146,59,0,0" Grid.Row="1" VerticalAlignment="Top" Width="75" />
        <Label Content="{Binding NewID}" HorizontalAlignment="Left" Margin="243,112,0,0" Grid.Row="1" VerticalAlignment="Top"/>
    </Grid>
</Window>
